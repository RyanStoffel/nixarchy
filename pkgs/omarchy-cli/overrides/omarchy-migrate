#!/usr/bin/env bash
# Nixarchy override: home-manager handles user-state migrations declaratively.
# omarchy:summary=No-op on Nixarchy; home-manager owns user-state migrations.

echo "Nixarchy: user-state migrations are managed by home-manager. Nothing to run."
exit 0
