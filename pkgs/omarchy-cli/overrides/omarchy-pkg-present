#!/usr/bin/env bash
# Nixarchy override: pacman not available; fall back to command -v.
# omarchy:summary=Returns true (exit 0) if all of the named commands are present.

for pkg in "$@"; do
  command -v "$pkg" >/dev/null 2>&1 || exit 1
done

exit 0
